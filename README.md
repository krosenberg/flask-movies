# flask-movies
A simple CRUD app for movie reviews

To run:

#### Install flask with pip

`pip install flask`

#### Run reset_db to initialize the database

`python reset_db.py`

#### Start the server

`python server.py`